CC=avr-gcc
OBJCOPY=avr-objcopy

CFLAGS=-Os -DF_CPU=16000000L -mmcu=atmega168
PORT=COM7

led.hex: led.elf
	${OBJCOPY} -O ihex -R .eeprom led.elf led.hex

led.elf: led.o
	${CC} -o led.elf led.o

install: led.hex
	avrdude -F -V -c arduino -p atmega168 -P ${PORT} -b 19200 -U flash:w:led.hex
